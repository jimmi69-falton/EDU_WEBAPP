# ===============================
# 🌐 SERVER CONFIGURATION
# ===============================
# Railway và Render tự động set PORT environment variable
# Spring Boot sẽ tự động đọc PORT nếu có, nếu không thì dùng 8080
server.port=${PORT:8080}

# ===============================
# 🗄️ DATABASE CONFIGURATION (PostgreSQL - Neon)
# ===============================
# Có thể override bằng environment variables: SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-orange-flower-a1d57mto-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_iFk1qnmd9PHv}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# ===============================
# 📥 DATABASE INITIALIZATION
# ===============================
# Có thể override bằng environment variables
spring.jpa.defer-datasource-initialization=${SPRING_JPA_DEFER_DATASOURCE_INIT:true}
spring.sql.init.mode=${SPRING_SQL_INIT_MODE:always}
spring.sql.init.platform=postgresql

# ===============================
# 🔧 JPA / HIBERNATE CONFIGURATION
# ===============================
# Có thể override bằng environment variables
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Cấu hình JWT
# ===============================
# Có thể override bằng environment variables: JWT_SECRET, JWT_EXPIRATION_MS
jwt.secret=${JWT_SECRET:day-la-mot-chuoi-bi-mat-rat-dai-va-an-toan-cho-hs512-ban-co-the-them-so-12345-va-ky-tu-dac-biet}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ===============================
# 🤖 GEMINI AI CONFIGURATION (OPTIONAL)
# ===============================
# Có thể override bằng environment variable: GEMINI_API_KEY
gemini.api.key=${GEMINI_API_KEY:AIzaSyCgL2z1tKWjet65nyov1tSVXl2f6oCLIJA}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent}

# ===============================
# 🌍 CORS CONFIGURATION
# ===============================
# Có thể override bằng environment variable: CORS_ALLOWED_ORIGINS
# SecurityConfig đã được cấu hình để cho phép tất cả origins (*) nên sẽ hoạt động với mọi frontend URL
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# ===============================
# 📰 NEWS API CONFIGURATION
# ===============================
newsapi.key=21e76d50f5d241c692d854558ba463d0

# ===============================
# 🧩 LOGGING
# ===============================
logging.level.com.hrmanagement=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.hrmanagement.service.AIService=DEBUG